library(shiny)

library(shiny)

ui <- fluidPage(
  excelOutput("test")
)

server <- function(input, output, session) {
  output$test <- renderExcel(excel(data = head(iris), columnDrag = TRUE))
}

shinyApp(ui, server)
